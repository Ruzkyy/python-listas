# LISTA dinámica, NO le especificamos elmentos
numeros = []

# Agregamos todos los elementos que queramos
numeros.append(8)     # Se asigna a la posición 0
numeros.append(10)    # Se asigna a la posición 1
numeros.append(5)     # Se asigna a la posición 2
numeros.append(4)     # Se asigna a la posición 3
numeros.append(2)     # Se asigna a la posición 4

# Imprimimos el elemento en la posición 2
print("El elemento en la posición 1 es: " + str(numeros[1]))

# Imprimimos el tamaño de la LISTA
print("El arreglo tiene " + str(len(numeros)) + " elementos")
